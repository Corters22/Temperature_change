var tempData = temp_data
var stdData = std_data
console.log("temp data", tempData)
console.log('std data', stdData)

function lineChart(filteredList) {

}

// TODO 
    // 1. line chart showing one country, all years, 12 lines for month of diff colors
    // 2. line chart showing one country, one year, months on x
    // 3. line chart showing one country, one month, all years
    // **grey out year or month if they pick the other**
    // chart showing hemisphere and/or continent
    // 4.get current demo of country from census data
    // 5. for month, avg out all the years